# STACK: React + TypeScript

---

I have a React + TypeScript production app. I've been given a large feature to build.

---

**FEATURE REQUEST:**
User will provide {paste the full feature description}

**ACCEPTANCE CRITERIA:**
User will provide {what does "done" look like — list every condition that must be true for this to be considered complete}

---

## Your Task

Do not write any code. Do not suggest any implementation. Surface every unknown before we touch anything.

1. **Hidden assumptions** — List every assumption buried in this requirement that you would have to make if you just started coding. For each one:
   - Is it safe to assume, or must I confirm with a human?
   - What goes wrong if the assumption is incorrect?

2. **High-risk ambiguities** — Classify each question:
   - 🔴 **Must clarify** — wrong assumption causes a bug, breaking change, or full rework
   - 🟡 **Should clarify** — wrong assumption causes a minor inconsistency

3. **Unaddressed edge cases** — What scenarios has the requirement not accounted for? Consider: empty state, loading, error, unauthenticated user, pagination, concurrent edits, mobile, permissions.

4. **Acceptance criteria gaps** — Are any acceptance criteria contradictory, unmeasurable, or unclear? Flag each one.

5. **React + TypeScript concerns** — Prop drilling risk, state ownership ambiguity, re-render risk, type safety gaps, shared state conflicts.

### Stop Condition
Only surface questions. Do not proceed to implementation thinking. I will answer everything before we move to the next step.
